<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailDeSession.PageAfficherClients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailDeSession"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="5 10 10 10" Spacing="12" Grid.Column="1">
            <Button x:Name="btnAjoutClient"
            Padding="16,10"
            CornerRadius="12"
            Background="#4CAF50"
            Foreground="White"
            Click="btnAjoutClient_Click">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="6">
                    <FontIcon Glyph="&#xE710;" />
                    <TextBlock Text="Ajouter" />
                </StackPanel>
            </Button>
        </StackPanel>

        <ScrollViewer Padding="20" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="30">

                <!-- Page Title -->
                <TextBlock Text="Liste des Clients"
                   HorizontalAlignment="Center"
                   x:Name="tbTitrePageAfficherClients"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="White"/>

                <!-- GridView -->
                <GridView ItemsSource="{x:Bind Clients}"
                  IsItemClickEnabled="True"
                  ItemClick="lvAfficher_ItemClick"
                  HorizontalAlignment="Center"
                  x:Name="lvbShowClients"
                  SelectionMode="None">

                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="local:Client">
                            <Border Width="160"
                            Padding="10"
                            Margin="5"
                            Background="#1E1E1E"
                            CornerRadius="10"
                            BorderBrush="#333"
                            BorderThickness="1">

                                <StackPanel Spacing="8" HorizontalAlignment="Center">

                                    <!-- Name -->
                                    <TextBlock Text="{x:Bind Nom}" 
                                       FontWeight="Bold"
                                       FontSize="14"
                                       HorizontalAlignment="Center"
                                       Foreground="White"
                                       TextWrapping="NoWrap"
                                       IsTextSelectionEnabled="True"/>

                                    <!-- Email -->
                                    <TextBlock Text="{x:Bind Email}" 
                                       TextWrapping="NoWrap"
                                       Width="140"
                                       FontSize="12"
                                       HorizontalAlignment="Center"
                                       Foreground="LightGray"
                                       IsTextSelectionEnabled="True"/>

                                    <!-- Afficher / Modifier Button -->
                                    <Border Width="140"
                                    Height="50"
                                    CornerRadius="6"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">

                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal"/>
                                                <VisualState x:Name="PointerOver">
                                                    <Storyboard>
                                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background" Storyboard.TargetName="BtnBorderClient">
                                                            <DiscreteObjectKeyFrame KeyTime="0" Value="#0D7A8C"/>
                                                        </ObjectAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>

                                        <Border x:Name="BtnBorderClient"
                                        Background="#0B99B2"
                                        CornerRadius="6">
                                            <TextBlock Text="Afficher&#x0a;Modifier"
                                               Foreground="White"
                                               FontWeight="Bold"
                                               FontSize="14"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               TextAlignment="Center"/>
                                        </Border>
                                    </Border>

                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>

                </GridView>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
