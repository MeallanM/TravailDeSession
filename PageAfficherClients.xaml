<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailDeSession.PageAfficherClients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailDeSession"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <MenuBar Grid.Row="0" HorizontalAlignment="right">
            <MenuBarItem Title="Ajouter">
            </MenuBarItem>
            <MenuBarItem Title="Modifier">
            </MenuBarItem>
            <MenuBarItem Title="Autres">
                <MenuFlyoutItem Text="Quitter" Tag="Quitter" />
            </MenuBarItem>
        </MenuBar>

        <ScrollViewer Padding="20" VerticalScrollBarVisibility="Auto" Grid.Row="1">
            <StackPanel Spacing="30">
                <TextBlock Text="Liste des Clients" FontSize="24" FontWeight="Bold"/>
                <GridView ItemsSource="{x:Bind Clients}"
              IsItemClickEnabled="True"
        ItemClick="lvAfficher_ItemClick"
        Height="300"
              x:Name="lvbShowClients">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="local:Client">
                            <StackPanel Margin="0,5">
                                <TextBlock Text="{x:Bind Nom}" FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind Email}"/>
                                <Border x:Name="BtnLikeText"
                                Width="140"
                                Height="40"
                                Background="#0B99B2"
                                CornerRadius="6"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">

                                <TextBlock Text="Afficher"
                                Foreground="White"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontWeight="Bold"
                                FontSize="14"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
