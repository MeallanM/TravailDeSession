<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailDeSession.PageAfficherClients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailDeSession"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

<<<<<<< Updated upstream
    <Grid>
        <ScrollViewer Padding="20" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="30">
                <TextBlock Text="Liste des Clients" FontSize="24" FontWeight="Bold"/>
                <GridView ItemsSource="{x:Bind Clients}"
              IsItemClickEnabled="True"
        ItemClick="lvAfficher_ItemClick"
        Height="300"
              x:Name="lvbShowClients">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="local:Client">
                            <StackPanel Margin="0,5">
                                <TextBlock Text="{x:Bind Nom}" FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind Email}"/>
                                <Button Content="Afficher" x:Name="btnAfficher"
                                Click="btnAfficher_Click"
                                Tag="{x:Bind}" />
                                <Button Content="Modifier" x:Name="btnModifier"
                                Click="btnModifier_Click"
                                Tag="{x:Bind}" />
                                <Button Content="Supprimer" x:Name="btnSupprimer"
                                Click="btnSupprimer_Click"
                                Tag="{x:Bind}" />
                            </StackPanel>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
=======
    <StackPanel>
        <TextBlock Text="List of Clients" FontSize="20" Margin="20"/>

        <!-- Add and Delete Buttons -->
        <Button Content="Add Client" Click="AddClient_Click"/>
        <Button Content="Delete Client" Click="DeleteClient_Click" Margin="10"/>
    </StackPanel>
>>>>>>> Stashed changes
</Page>
