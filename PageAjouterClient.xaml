<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailDeSession.PageAjouterClient"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailDeSession"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Nom -->
        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Text="Nom :" VerticalAlignment="Center" FontSize="14"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
                 x:Name="txtNom" Margin="5" FontSize="14"/>

        <!-- Adresse -->
        <TextBlock Grid.Row="1" Grid.Column="0" 
                   Text="Adresse :" VerticalAlignment="Center" FontSize="14"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 x:Name="txtAdresse" Margin="5" FontSize="14"/>

        <!-- Téléphone -->
        <TextBlock Grid.Row="2" Grid.Column="0" 
                   Text="Téléphone :" VerticalAlignment="Center" FontSize="14"/>
        <TextBox Grid.Row="2" Grid.Column="1" 
                 x:Name="txtTelephone" Margin="5" FontSize="14"/>

        <!-- Email -->
        <TextBlock Grid.Row="3" Grid.Column="0" 
                   Text="Email :" VerticalAlignment="Center" FontSize="14"/>
        <TextBox Grid.Row="3" Grid.Column="1"
                 x:Name="txtEmail" Margin="5" FontSize="14"/>

        <!-- Bouton Créer -->
        <Button Grid.Row="4" Grid.Column="1"
                Content="Créer le Client"
                Margin="5" Width="150" Height="35"
                HorizontalAlignment="Right"
                Click="ClientCreation_Click"/>
    </Grid>
</Page>
