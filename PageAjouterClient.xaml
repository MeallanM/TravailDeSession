<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailDeSession.PageAjouterClient"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailDeSession"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0 5">
            <TextBlock Text="Nom :" FontSize="14"/>
            <TextBox x:Name="txtNom" Margin="0 3" FontSize="14"/>
            <TextBlock x:Name="tbxErrorNom" 
                   Text="Veuillez entrer un nom valide"
                   Foreground="Red" Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0 5">
            <TextBlock Text="Adresse :" FontSize="14"/>
            <TextBox x:Name="txtAdresse" Margin="0 3" FontSize="14"/>
            <TextBlock x:Name="tbxErrorAdresse"
                   Text="Veuillez entrer une adresse valide"
                   Foreground="Red" Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0 5">
            <TextBlock Text="Téléphone :" FontSize="14"/>
            <TextBox x:Name="txtTelephone" Margin="0 3" FontSize="14"/>
            <TextBlock x:Name="tbxErrorTelephone"
                   Text="Veuillez entrer un numéro de téléphone valide"
                   Foreground="Red" Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0 5">
            <TextBlock Text="Email :" FontSize="14"/>
            <TextBox x:Name="txtEmail" Margin="0 3" FontSize="14"/>
            <TextBlock x:Name="tbxErrorEmail"
                   Text="Veuillez entrer un email valide"
                   Foreground="Red" Visibility="Collapsed"/>
        </StackPanel>

        <Button Grid.Row="4" Grid.Column="1"
            Content="Créer le Client"
            Margin="5" Width="150" Height="35"
            HorizontalAlignment="Right"
            Click="ClientCreation_Click"/>
    </Grid>
</Page>
