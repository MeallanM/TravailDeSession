<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailDeSession.PageAfficherEmploye"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailDeSession"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel HorizontalAlignment="Right" Spacing="12" Grid.Row="0">
            <Button x:Name="btnAjouterEmploye" Click="btnAjouterEmploye_Click"
            Padding="16,10"
            CornerRadius="12"
            Background="#4CAF50"
            Foreground="White">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="6">
                    <FontIcon Glyph="&#xE710;" />
                    <TextBlock Text="Add" />
                </StackPanel>
            </Button>
        </StackPanel>
        <TextBlock Text="Liste des Employés" FontSize="24" FontWeight="Bold"/>
        <ListView ItemsSource="{x:Bind Employes}"
IsItemClickEnabled="True"
  ItemClick="lvAfficher_ItemClick"
  Height="300"
x:Name="lvbShowEmployes">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Employe">
                    <StackPanel Margin="0,5">
                        <TextBlock Text="{x:Bind Nom}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Prenom}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Email}"/>
                        <Image Source="{x:Bind PhotoIdentiteImage}" Height="80"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
